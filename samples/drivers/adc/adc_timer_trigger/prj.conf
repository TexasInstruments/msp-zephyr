# ADC driver configuration
CONFIG_ADC=y

# Console configuration for output
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# Logging configuration
CONFIG_LOG=y
CONFIG_ADC_LOG_LEVEL_INF=y

# Counter/Timer driver (used for triggering)
CONFIG_COUNTER=y
